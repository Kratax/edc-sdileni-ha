# Ukázkové Lovelace karty pro EDC sdílení elektřiny
# =================================================
#
# Všude níž nahraď EAN v entity ID za svůj. Skutečné entity si najdeš
# v Nástroje pro vývojáře -> Stavy, filtr "edc".
#
#   sensor.edc_<EAN>_vyroba_export             kWh, výroba za poslední uzavřený den
#   sensor.edc_<EAN>_uspesne_sdileno           kWh, z toho úspěšně sdíleno
#   sensor.edc_<EAN>_podil_sdilene_elektriny   %
#
# Grafy níž nečtou recorder, ale atribut `historie_dni` obou energetických
# entit. Ten obsahuje celou dosud známou historii po dnech:
#
#   {"2026-08-01": {"measured": 24.1, "shared": 21.2}, ...}
#
# Praktický důsledek: graf se vykreslí kompletní hned po instalaci, včetně
# historie doplněné zpětně z portálu, a nezávisí na tom, jak dlouho a jak
# podrobně ti recorder drží data.
#
# Karty s `type: custom:apexcharts-card` potřebují ApexCharts Card
# (HACS -> Frontend -> ApexCharts Card). Poslední příklad ApexCharts nepotřebuje.


# ---------------------------------------------------------------------------
# 1) Skládaný sloupcový graf: export po dnech, uvnitř sdílená část
# ---------------------------------------------------------------------------
# Pozor na past: naskládat na sebe "export" a "sdíleno" by tu samou elektřinu
# počítalo dvakrát, protože sdíleno je podmnožina exportu. Proto se skládá
# `sdíleno` + `nesdíleno` (= export - sdíleno) a výška celého sloupce pak
# odpovídá celkovému exportu.

type: custom:apexcharts-card
graph_span: 30d
span:
  end: day
stacked: true
header:
  show: true
  title: Export a sdílení po dnech
  show_states: false
apex_config:
  chart:
    height: 320
  plotOptions:
    bar:
      columnWidth: 70%
      borderRadius: 4
  legend:
    position: top
    horizontalAlign: left
  dataLabels:
    enabled: false
  tooltip:
    shared: true
    intersect: false
  yaxis:
    decimalsInFloat: 1
series:
  - name: Sdíleno
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#1baf7a"
    unit: " kWh"
    float_precision: 2
    data_generator: |
      const h = entity.attributes.historie_dni || {};
      return Object.keys(h).sort().map(d =>
        [new Date(d + "T12:00:00").getTime(), h[d].shared]);
  - name: Nesdíleno
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#898781"
    unit: " kWh"
    float_precision: 2
    data_generator: |
      const h = entity.attributes.historie_dni || {};
      return Object.keys(h).sort().map(d =>
        [new Date(d + "T12:00:00").getTime(), Math.max(0, h[d].measured - h[d].shared)]);

# `new Date(d + "T12:00:00")` je záměr, ne překlep. Datum bez času se parsuje
# jako UTC midnight a ve středoevropském letním čase by sloupec spadl o den
# vedle. Poledne má k obojí hranici dne dost rezervy.
#
# Detail jednoho dne po 15 minutách je v příkladu 6).


# ---------------------------------------------------------------------------
# 2) Totéž po měsících — přehled za celý rok
# ---------------------------------------------------------------------------
# Stejná data, jen posčítaná do měsíců. Hodí se, když už máš historie víc než
# pár týdnů a denní sloupce se přestávají dát přečíst.

type: custom:apexcharts-card
graph_span: 365d
span:
  end: day
stacked: true
header:
  show: true
  title: Export a sdílení po měsících
  show_states: false
apex_config:
  chart:
    height: 320
  plotOptions:
    bar:
      columnWidth: 60%
      borderRadius: 4
  legend:
    position: top
    horizontalAlign: left
  dataLabels:
    enabled: false
  tooltip:
    shared: true
    intersect: false
series:
  - name: Sdíleno
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#1baf7a"
    unit: " kWh"
    float_precision: 0
    group_by:
      func: sum
      duration: 1month
    data_generator: |
      const h = entity.attributes.historie_dni || {};
      return Object.keys(h).sort().map(d =>
        [new Date(d + "T12:00:00").getTime(), h[d].shared]);
  - name: Nesdíleno
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#898781"
    unit: " kWh"
    float_precision: 0
    group_by:
      func: sum
      duration: 1month
    data_generator: |
      const h = entity.attributes.historie_dni || {};
      return Object.keys(h).sort().map(d =>
        [new Date(d + "T12:00:00").getTime(), Math.max(0, h[d].measured - h[d].shared)]);


# ---------------------------------------------------------------------------
# 3) Celá sekce dashboardu: čísla + graf
# ---------------------------------------------------------------------------
# Vlož jako jednu kartu (Přidat kartu -> Ruční). Podíl sdílení je schválně
# samostatná dlaždice a ne křivka v grafu — mělo by to jinou jednotku a
# druhá osa dělá ze skládaného grafu nepřehlednou kaši.

type: vertical-stack
cards:
  - type: glance
    title: EDC sdílení — poslední uzavřený den
    show_name: true
    show_state: true
    columns: 3
    entities:
      - entity: sensor.edc_859182400312427576_vyroba_export
        name: Export
      - entity: sensor.edc_859182400312427576_uspesne_sdileno
        name: Sdíleno
      - entity: sensor.edc_859182400312427576_podil_sdilene_elektriny
        name: Podíl

  - type: custom:apexcharts-card
    graph_span: 30d
    span:
      end: day
    stacked: true
    header:
      show: false
    apex_config:
      chart:
        height: 260
      plotOptions:
        bar:
          columnWidth: 70%
          borderRadius: 4
      legend:
        position: top
        horizontalAlign: left
      dataLabels:
        enabled: false
      tooltip:
        shared: true
        intersect: false
    series:
      - name: Sdíleno
        entity: sensor.edc_859182400312427576_vyroba_export
        type: column
        color: "#1baf7a"
        unit: " kWh"
        float_precision: 2
        data_generator: |
          const h = entity.attributes.historie_dni || {};
          return Object.keys(h).sort().map(d =>
            [new Date(d + "T12:00:00").getTime(), h[d].shared]);
      - name: Nesdíleno
        entity: sensor.edc_859182400312427576_vyroba_export
        type: column
        color: "#898781"
        unit: " kWh"
        float_precision: 2
        data_generator: |
          const h = entity.attributes.historie_dni || {};
          return Object.keys(h).sort().map(d =>
            [new Date(d + "T12:00:00").getTime(), Math.max(0, h[d].measured - h[d].shared)]);


# ---------------------------------------------------------------------------
# 4) Víc EANů v jednom grafu
# ---------------------------------------------------------------------------
# Skládá se export po EANech. Sdílenou část tady vidět nejde — jedna osa
# neunese dvě dělení současně. Když chceš u každého EANu i sdílení, dej
# vedle sebe víc grafů z příkladu 1.

type: custom:apexcharts-card
graph_span: 30d
span:
  end: day
stacked: true
header:
  show: true
  title: Export po EANech
  show_states: false
apex_config:
  chart:
    height: 320
  plotOptions:
    bar:
      columnWidth: 70%
      borderRadius: 4
  legend:
    position: top
    horizontalAlign: left
  dataLabels:
    enabled: false
  tooltip:
    shared: true
    intersect: false
series:
  - name: EAN ...427576
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#2a78d6"
    unit: " kWh"
    float_precision: 2
    data_generator: |
      const h = entity.attributes.historie_dni || {};
      return Object.keys(h).sort().map(d =>
        [new Date(d + "T12:00:00").getTime(), h[d].measured]);
  - name: EAN ...999999
    entity: sensor.edc_859182400312999999_vyroba_export
    type: column
    color: "#eb6834"
    unit: " kWh"
    float_precision: 2
    data_generator: |
      const h = entity.attributes.historie_dni || {};
      return Object.keys(h).sort().map(d =>
        [new Date(d + "T12:00:00").getTime(), h[d].measured]);


# ---------------------------------------------------------------------------
# 5) Bez ApexCharts — vestavěná statistics-graph karta
# ---------------------------------------------------------------------------
# Když nechceš instalovat další kartu z HACS. Omezení, se kterými je potřeba
# počítat: skládat sloupce neumí (obě série jsou vedle sebe), a kreslí jen to,
# co má v dlouhodobých statistikách — tedy od instalace integrace dál, ne
# historii doplněnou zpětně z portálu.

type: statistics-graph
title: Export a sdílení po dnech
chart_type: bar
period: day
days_to_show: 30
stat_types:
  - sum
entities:
  - entity: sensor.edc_859182400312427576_vyroba_export
    name: Export
  - entity: sensor.edc_859182400312427576_uspesne_sdileno
    name: Sdíleno


# ---------------------------------------------------------------------------
# 6) Detail předchozího dne po 15 minutách
# ---------------------------------------------------------------------------
# Stejná struktura jako příklad 1, jen místo dnů jsou 15minutové intervaly
# jednoho dne. Čte atribut `detail_15min` entity výroby, který obsahuje tři
# souběžná pole:
#
#   {"datum": "2026-08-02",
#    "casy":    ["00:00", "00:15", ...],   96 položek
#    "vyroba":  [0.01, 0.01, ...],
#    "sdileno": [0.0, 0.0, ...]}
#
# Časy jsou z odpovědi portálu, ne dopočítané jako index * 15 minut — v den
# změny času má den 92 nebo 100 intervalů a dopočítané časy by se po přechodu
# rozešly o hodinu.
#
# Atribut je jen na entitě výroby (`..._vyroba_export`), protože obsahuje obě
# série. Na entitě sdílení není, aby se ta samá data nezdvojovala.

type: custom:apexcharts-card
graph_span: 1d
span:
  end: day
  offset: -1d
stacked: true
header:
  show: true
  title: Předchozí den po 15 minutách
  show_states: false
apex_config:
  chart:
    height: 300
  plotOptions:
    bar:
      columnWidth: 100%
      borderRadius: 1
  legend:
    position: top
    horizontalAlign: left
  dataLabels:
    enabled: false
  stroke:
    width: 0
  tooltip:
    shared: true
    intersect: false
    x:
      format: HH:mm
  xaxis:
    labels:
      format: HH:mm
  yaxis:
    decimalsInFloat: 2
series:
  - name: Sdíleno
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#1baf7a"
    unit: " kWh"
    float_precision: 3
    data_generator: |
      const d = entity.attributes.detail_15min;
      if (!d || !d.casy) return [];
      return d.casy.map((t, i) => {
        const [h, m] = t.split(":").map(Number);
        const ts = new Date(d.datum + "T00:00:00");
        ts.setHours(h, m, 0, 0);
        return [ts.getTime(), d.sdileno[i]];
      });
  - name: Nesdíleno
    entity: sensor.edc_859182400312427576_vyroba_export
    type: column
    color: "#898781"
    unit: " kWh"
    float_precision: 3
    data_generator: |
      const d = entity.attributes.detail_15min;
      if (!d || !d.casy) return [];
      return d.casy.map((t, i) => {
        const [h, m] = t.split(":").map(Number);
        const ts = new Date(d.datum + "T00:00:00");
        ts.setHours(h, m, 0, 0);
        return [ts.getTime(), Math.max(0, d.vyroba[i] - d.sdileno[i])];
      });

# `offset: -1d` posouvá okno grafu na včerejšek, což je den, který integrace
# normálně reportuje. Kdyby ti portál data uzavíral s větším zpožděním, sloupce
# by vypadly z okna a graf by byl prázdný — pak zvětši offset (`-2d`).
# Skutečné datum, ke kterému data patří, je vždy v `detail_15min.datum` a
# v atributu `datum` entity.
